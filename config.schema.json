{
  "pluginAlias": "SolisSensors",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "keyId": {
        "title": "ID key",
        "type": "string",
        "required": true,
        "default": "Solis id key"
      },
      "keySecret": {
        "title": "Secret key",
        "type": "string",
        "required": true,
        "default": "Solis secret key"
      },
      "stationId": {
        "title": "Station ID",
        "type": "string",
        "required": true,
        "default": "Solis station ID"
      }
    }
  }
}